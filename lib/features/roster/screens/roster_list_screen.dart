import 'package:flutter/material.dart';

class RosterListScreen extends StatelessWidget {
  const RosterListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Roster')),
      body: const Center(child: Text('Roster — coming soon')),
    );
  }
}
